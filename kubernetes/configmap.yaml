apiVersion: v1
kind: ConfigMap
metadata:
  name: msvc-usuarios
data:
  lb_auth_issuer_uri: http://127.0.0.1:9000
  lb_usuarios_uri: http://127.0.0.1:8001
  port: "8001"
  db_host: "mysql:3306"
  database: msvc_usuarios
  cursos_url: "msvc-cursos:8002"
  application.yaml: |-
    config:
      texto: Configurando ambiente por defecto
    ---
    spring:
      config:
        activate:
          on-profile: dev
    config:
      texto: Configurando ambiende desarrollo
    ---
    spring:
      config:
        activate:
          on-profile: prod
    config:
      texto: Configurando ambiende de producción
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: msvc-cursos
data:
  port: "8002"
  db_host: "postgres:5432"
  database: msvc_cursos
  usuarios_url: "msvc-usuarios:8001"